<!DOCTYPE svg PUBLIC "-//W3C//DTD SVG 1.1//EN" "http://www.w3.org/Graphics/SVG/1.1/DTD/svg11.dtd">
<svg version="1.1" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink"
 width="1000" height="1000" viewBox="-250 -250 500 500" onload="init(evt)" onclick="togglePause(evt)">
 <title>SVG animation using ECMAScript</title>
 <script type="text/ecmascript">
  var stop=true;
  var intervalId;
  var frameId = -1;
  var totalDuration = 1000;
  var totalFrame = totalDuration/16;
  var object;
  
  function init(evt) {
   object = evt.target.ownerDocument.getElementById('rot');
   
   togglePause(evt);
  }
  
  function animate() {
     if(++frameId>totalFrame) frameId=0;
	 object.setAttribute('r',100.0*frameId/totalFrame);
     
	 if(!stop) requestAnimationFrame(animate);
    }
  
  function togglePause(evt) {
   if(stop) {
    stop=false;
	requestAnimationFrame(animate);
   } else {
    stop=true;
   }
  }
 </script>
 <line id="l1" x1="-50" y1="0" x2="50" y2="0" stroke="gray"/>
 <circle id="p0" cx="-50" cy="0" r="1.25" stroke="gray" fill="white"/>
 <circle id="p1" cx="50" cy="0" r="1.25" stroke="gray" fill="white"/>
 <circle id="rot" cx="0" cy="0" r="0" stroke="green" fill="none"/>
 <line x1="-100" y1="-100" x2="100" y2="100" stroke="red"/>
</svg>